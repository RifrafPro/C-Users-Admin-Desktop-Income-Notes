# Session — 2026-09-16 (cloud: found the progress-loss mechanism · resume protocol installed · backup queued)

## Where we left off (read first next session)
1. **THE "LOSING PROGRESS" MYSTERY IS SOLVED.** This cloud session sat **18 days on a stale
   vault** — 19 commits of desktop work (Sept 3–7) existed on GitHub that cloud never pulled:
   Chain Bridge deal work, the four autonomy rulings, VA/MD assignment law, call-agent
   knowledge base, the 09-07 honest assessment. Nothing was ever lost from GitHub — sessions
   were reading OLD state and re-treading ground. Fix installed (see #2). Full write-up:
   **`vault/resume-protocol.md`** (now standing policy, linked from CLAUDE.md).
2. **Sync-first hook LIVE:** `.claude/hooks/vault-load.sh` now fetches + auto-pulls at every
   session start and prints 🚨 loudly if the vault is stale or has unpushed commits. It
   already worked once this session (auto-pulled after a resume).
3. **⬜ Job 008 queued (PRIORITY 1, run before all other jobs):** desktop runs
   `tools/backup-vault.ps1` (new) → dated full snapshot + full-history git bundle into
   **Desktop\Income Notes - Backup**, keeps newest 10, plus registers a nightly 9pm
   Task Scheduler run. Rich says "check your inbox" on the desktop to trigger it.
4. **Backup redundancy map (honest version):** full history = GitHub + the local backup
   folder's `.bundle` files (Job 008, nightly). Drive = readable text mirror of key files
   (the connector can't reliably carry binary bundles — attempted 09-16, dropped by design).
   Restore from any of: GitHub clone, `git clone file.bundle`, or plain snapshot folder.
5. **From the 09-07 honest assessment (desktop): EXECUTION GATE is in force** — no new
   research/tools/extractions until **D1 (one builder call) is done.** Paramount
   301-370-6463 · Botero 202-515-0020 · Paradigm 703-476-5877. Also still open: Bruce
   Goudarzi 703-378-8810 re: 2754 Chain Bridge (why the contract died · both lots platted? ·
   financing terms), Propwire account, Jobs 004–007, Chesterbrook plat date (needs Chrome
   extension → Fairfax GIS).
6. Rich also referenced "a local Claude setup launched, used and controlled here on this
   computer" — that EXISTS and works (since 08-28/09-03: desktop app + CLI, push fixed).
   The resume protocol makes the two surfaces safe to interleave.

## What we did today
- Pulled the 19 stale commits; read the 09-03 session log + 09-07 honest assessment; fully
  reconciled 18 days of desktop work into cloud context.
- Diagnosed the recurring "steps backward" feeling → stale-session mechanism (above).
- **Installed the bulletproof resume system:**
  - `vault/resume-protocol.md` — the one law (pull first, push last, unpushed ≠ exists) +
    5 backup layers + session choreography + restore drill.
  - Sync-first block in `vault-load.sh` (auto-pull, loud staleness/unpushed warnings).
  - `tools/backup-vault.ps1` — Windows snapshot script (robocopy full copy + git bundle,
    10-deep rotation, finds/creates the backup folder).
  - Job 008 in `inbox-for-desktop.md` (run backup + register nightly task).
  - CLAUDE.md memory-system section updated to point at all of it.
- Mirrored resume-protocol.md + this session log to Drive; created "backups" subfolder in
  the Drive vault with a manifest pointing at where the real full-history copies live.
