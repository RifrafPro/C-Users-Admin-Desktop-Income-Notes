# Session — 2026-09-17 (cloud: wrong-folder trap found · one-click installer delivered · install NOT yet confirmed)

## Where we left off (read first next session)
1. **INSTALL STATUS: NOT CONFIRMED.** The one-shot fixer (`tools/install-courier.ps1`)
   has NOT run on Rich's PC yet — no push from the desktop since Job 008 (96244a5,
   09-16 3:47pm PT). Its final step pushes a log commit titled "install-courier: trap
   removed…" — **check origin for that commit first thing.** If it landed: courier is
   live, Jobs 009/010 done, bridge automatic. If not: Rich has a downloaded
   **RUN-ME-FIX-EVERYTHING.cmd** (sent via chat file card 09-17 night) — double-click
   is all that's left. Don't re-explain; just ask if the window said ALL DONE.
2. **THE RECURRING FAILURE, NAILED:** Rich's terminal keeps opening Claude in the OUTER
   `Desktop\Income Notes` folder — a stray remote-less git repo left from the August
   login saga. The real vault is NESTED inside it: `Income Notes\
   C-Users-Admin-Desktop-Income-Notes` (the desktop's own Job 008 commit documented
   this). Sessions born in the outer folder have no CLAUDE.md, no hook, no remote →
   "doesn't sync, doesn't understand." Job 010 + the installer remove the trap and put
   a **Claude Vault** Desktop icon (always opens the real repo).
3. **What the installer does when it runs:** archives+deletes outer `.git`, signposts
   the outer folder, installs the Desktop launcher, registers **courier tasks 8am/6pm**
   (headless `claude -p` inbox runs via `tools/courier-run.cmd`), marks Jobs 009/010
   done, commits + pushes its own log (`vault/courier-install-log.txt`).
4. **Message bus is two-way now:** `vault/inbox-for-cloud.md` (desktop→cloud) exists;
   cloud's hook prints its OPEN msgs at session start. Repo `.claude/settings.json`
   has a permission allowlist so headless runs never stall on prompts.
5. **Rich's frustration level: high.** He believed cloud Claude could see/control his
   PC screen and mic ("I've given you the tools"). Clarified honestly: web/video tools
   reach the public internet only; the vault is the only pipe into his PC; screenshots
   (Win+Shift+S → paste in chat) are the fast way to show me his terminal. Model given:
   **two bodies, one brain** — cloud (thinking/connectors) + local (PC hands), joined
   by the vault. Keep answers short, do-it-for-him, zero option menus.
6. **Rich's 5-step frame** (market research → buyers+contracts → seller outreach →
   terms/contract → close) recorded in road-to-deal-one.md with per-step status. The
   business gap is unchanged: seam between steps 2 and 3 — **D1 builder calls +
   Goudarzi Chain Bridge call.** Execution gate (09-07) still in force.

## What we did today (cloud)
- Diagnosed and closed the wrong-folder trap (Job 010, claude-vault.cmd launcher).
- Built the automatic courier (Job 009, courier-run.cmd, 8am/6pm) + inbox-for-cloud
  return channel + permission allowlist + one-shot installer install-courier.ps1
  (ASCII-safe for PS 5.1 after last night's encoding lesson).
- Delivered RUN-ME-FIX-EVERYTHING.cmd to Rich as a downloadable double-click file.
- Two timed self-checks watched for the install push (none arrived by 9:15pm ET).
- Answered the webinar ask from vault memory (Flipping Mastery extraction, 09-03) —
  no new extraction, execution gate respected.

## Tomorrow's first moves (cloud)
1. `git fetch` → look for the install-courier commit. Confirm or gently re-offer the
   RUN-ME file (one line, no lecture).
2. If courier is live: watch for the 8am courier commit as proof of the loop.
3. Keep steering to the two phone calls (D1 builders · Goudarzi) — the machine is done
   enough; the money is in step 3.
